#!/bin/bash
 
```
 In this lesson if you want to run ts-node :
 1. Install ts-node by using this command: "npm i typescript -p ts-node"
 2. Use shell script to run the current file or project
 "./script"

```